{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cordiverse-paper.serviceAccountName" . }}
  labels:
    {{- include "cordiverse-paper.labels" . | nindent 4 }}
{{- end }}
