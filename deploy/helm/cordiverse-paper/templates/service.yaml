apiVersion: v1
kind: Service
metadata:
  name: {{ include "cordiverse-paper.fullname" . }}
  labels:
    {{- include "cordiverse-paper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cordiverse-paper.selectorLabels" . | nindent 4 }}
