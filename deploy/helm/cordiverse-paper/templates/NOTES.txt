已安装 {{ include "cordiverse-paper.fullname" . }}。

1. 在 Rancher 中：
   - 工作负载 → Deployment「{{ include "cordiverse-paper.fullname" . }}」
   - 服务发现 → Service「{{ include "cordiverse-paper.fullname" . }}」

2. 用 Service 端口转发：
   kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "cordiverse-paper.fullname" . }} 8080:{{ .Values.service.port }}

镜像需已推送到：{{ .Values.image.repository }}:{{ .Values.image.tag }}
